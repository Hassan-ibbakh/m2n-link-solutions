import { Mail, MapPin, Phone } from "lucide-react";
import { ContactForm } from "@/components/contact-form";
import { Footer, Header, Reveal } from "@/components/site-shell";
import { site } from "@/lib/content";

export const metadata = { title: "Contact", description: "Contactez M2N Link Solutions pour vos projets web, mobile, AI et transformation digitale." };

export default function ContactPage() { return <><Header /><main><section className="bg-coral px-5 pb-24 pt-40 sm:px-8 sm:pb-32 sm:pt-52"><div className="mx-auto max-w-7xl"><p className="label text-ink/60">Demarrer un projet</p><h1 className="mt-6 max-w-5xl font-display text-6xl leading-[.92] tracking-tight sm:text-8xl">Parlons de ce que vous voulez rendre possible.</h1></div></section><section className="px-5 py-24 sm:px-8 sm:py-32"><div className="mx-auto grid max-w-7xl gap-16 lg:grid-cols-[.7fr_1.3fr]"><Reveal><p className="max-w-sm text-xl leading-relaxed">M2N Link Solutions vous accompagne dans la conception de produits web, mobiles et intelligents, de l&apos;idee au lancement.</p><div className="mt-12 grid gap-5 text-sm"><a className="flex items-center gap-3 text-ink/60 hover:text-coral" href={`mailto:${site.email}`}><Mail size={18} />{site.email}</a><a className="flex items-center gap-3 text-ink/60 hover:text-coral" href={`mailto:${site.secondaryEmail}`}><Mail size={18} />{site.secondaryEmail}</a><a className="flex items-center gap-3 text-ink/60 hover:text-coral" href={`tel:${site.phone.replaceAll(" ", "")}`}><Phone size={18} />{site.phone}</a><a className="flex items-start gap-3 text-ink/60 hover:text-coral" href={site.mapUrl} target="_blank" rel="noreferrer"><MapPin className="mt-0.5 shrink-0" size={18} /><span>{site.address}</span></a></div></Reveal><Reveal><ContactForm /></Reveal></div></section></main><Footer /></> }
