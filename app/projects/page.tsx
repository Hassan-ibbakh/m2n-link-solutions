import { ArrowUpRight } from "lucide-react";
import { Callout, ProjectsGrid, SectionIntro } from "@/components/sections";
import { Footer, Header, Reveal } from "@/components/site-shell";
import { projects } from "@/lib/content";

export const metadata = { title: "Projects", description: "Selected AI, data, and software projects built with Nexa Intelligence." };

export default function ProjectsPage() { return <><Header /><main><section className="px-5 pb-24 pt-40 sm:px-8 sm:pb-32 sm:pt-52"><div className="mx-auto max-w-7xl"><p className="label text-coral">Selected work</p><h1 className="mt-6 max-w-4xl font-display text-6xl leading-[.95] tracking-tight sm:text-8xl">Good ideas deserve good execution.</h1><p className="mt-10 max-w-xl text-lg leading-relaxed text-ink/60">A look at a few partnerships where thoughtful technology made the difference.</p></div></section><section className="px-5 pb-24 sm:px-8 sm:pb-32"><div className="mx-auto max-w-7xl"><ProjectsGrid /><div className="mt-28"><SectionIntro eyebrow="Inside the work" title="Every project starts with a better question." /><div className="mt-14 grid gap-8 md:grid-cols-3">{projects.map((project) => <Reveal key={project.title}><article className="border-t border-ink/20 pt-5"><p className="label text-coral">{project.category}</p><h2 className="mt-6 font-display text-2xl">{project.problem}</h2><p className="mt-4 text-sm leading-relaxed text-ink/60">{project.solution}</p><div className="mt-8 flex items-center gap-2 text-sm font-semibold">{project.result}<ArrowUpRight size={15} /></div></article></Reveal>)}</div></div></div></section><Callout /></main><Footer /></> }
