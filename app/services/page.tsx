import { ArrowUpRight } from "lucide-react";
import { Callout, SectionIntro } from "@/components/sections";
import { Footer, Header, Reveal } from "@/components/site-shell";
import { services } from "@/lib/content";

export const metadata = { title: "Services", description: "Web and mobile app development, AI, software, data, and digital product capabilities from Nexa Intelligence." };

export default function ServicesPage() { return <><Header /><main><section className="px-5 pb-24 pt-40 sm:px-8 sm:pb-32 sm:pt-52"><div className="mx-auto max-w-7xl"><p className="label text-coral">Capabilities</p><h1 className="mt-6 max-w-5xl font-display text-6xl leading-[.95] tracking-tight sm:text-8xl">Web, mobile, AI, and software built for real work.</h1><p className="mt-10 max-w-xl text-lg leading-relaxed text-ink/60">From an idea worth testing to a platform worth scaling, we bring the range and rigor to move with confidence across every screen and system.</p></div></section><section className="bg-mint px-5 py-24 sm:px-8 sm:py-32"><div className="mx-auto max-w-7xl"><SectionIntro eyebrow="What we do" title="Deep expertise, applied with judgment." /><div className="mt-14 grid gap-4">{services.map((service) => { const Icon = service.icon; return <Reveal key={service.title}><article className="group grid gap-6 border-t border-ink/20 py-8 md:grid-cols-[.15fr_.4fr_1fr_auto] md:items-start"><span className="text-sm text-coral">{service.eyebrow}</span><div className="flex items-center gap-4"><Icon size={25} strokeWidth={1.5} /><h2 className="font-display text-2xl">{service.title}</h2></div><div><p className="max-w-lg text-sm leading-relaxed text-ink/60">{service.description}</p><div className="mt-5 flex flex-wrap gap-2">{service.capabilities.map((capability) => <span key={capability} className="rounded-full border border-ink/20 px-3 py-1 text-xs">{capability}</span>)}</div></div><a href="/contact" aria-label={`Discuss ${service.title}`} className="grid h-10 w-10 place-items-center rounded-full border border-ink transition-transform group-hover:translate-x-1"><ArrowUpRight size={16} /></a></article></Reveal>})}</div></div></section><Callout /></main><Footer /></> }
